// Now we have expressions and two dimensional arrays.
// You can see an error here that variable `x` was not
// declared in this scope

func foo() {
  return 42;
}

func app() {
  var a = 1;
  var b = -x;
  var c = a == b;
  var d = a + b * c;
  var e = (a + b) * c;

  array arr[5][10];
  arr[1][1] = 42;

  var f = e < arr[1][1];
  var g = foo();

  return 0;
}
